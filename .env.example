# Local-only credentials. Copy to `.env` or export in your shell.
LINEAR_API_KEY=

# API runtime.
APP_ENV=local
LEGAL_SCOPE=labor_law
DATABASE_URL=postgresql://legalrag:legalrag@localhost:5432/legalrag

# Required for LLM-backed runtime features. Must point to an OpenAI-compatible
# /v1 chat-completions API.
OPENAI_COMPATIBLE_BASE_URL=https://api.openai.com/v1
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_CHAT_MODEL=
OPENAI_COMPATIBLE_TIMEOUT_SECONDS=60
